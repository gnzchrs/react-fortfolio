import React from 'react'

const About = (props) => {
  return (
    <div
      id={props.id}
      style={{
        height: '100vh',
        display: 'flex',
        alignItems: 'center',
        justifyContent: 'center',
        backgroundColor: 'white'
      }}></div>
  )
}

export default About
